#include "system.h"
#include "prototypes.h"

//Package 8.3 - Network solution, compute pipe flows

void	compute_pipe_flows(  int num_pipes, matrix <int> node_table, vector <double> heads, \
                          vector <double> &pipe_flows, vector <double> f_factors, bool debug)
{
  cout << "final solve step not yet implemented but it's gonna be dope" ;
  return ;
}
